#build_prefix: extra-x86_64
maintainers:
  - github: starsareintherose
    email: kuoi@bioarchlinux.org
pre_build_script: |
  update_pkgrel()
  vcs_update()
post_build_script: |
  git_add_files('PKGBUILD')
  git_pkgbuild_commit()
  git_commit()
supdate_on:
  - source: github
    github: archlinuxcn/devtools
#managed: false
