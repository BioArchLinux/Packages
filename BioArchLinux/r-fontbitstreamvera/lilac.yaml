build_prefix: extra-x86_64
maintainers:
  - github: sukanka
post_build_script: |
  git_pkgbuild_commit()
update_on:
  - regex: fontBitstreamVera_([\d._-]+).tar.gz
    source: regex
    url: https://cran.r-project.org/package=fontBitstreamVera
  - alias: r
