build_prefix: extra-x86_64
maintainers:
  - github: starsareintherose
    email: kuoi@bioarchlinux.org
post_build_script: |
  git_add_files('PKGBUILD')
  git_commit()
  update_aur_repo()
update_on:
  - regex: pvm(\d+.\d+.\d+).tgz
    source: regex
    url: https://netlib.org/pvm3/
