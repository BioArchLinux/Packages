_name=nd2reader
pkgname=python-nd2reader
pkgver=3.3.1
pkgrel=2
pkgdesc="A tool for reading ND2 files produced by NIS Elements"
arch=(any)
url="https://open-science-tools.github.io/nd2reader/"
license=(GPL-3.0-or-later)
depends=('python-numpy' 'python-xmltodict' 'python-pims')
makedepends=('python-setuptools' 'python-setuptools')
source=("https://files.pythonhosted.org/packages/source/${_name::1}/${_name}/nd2reader-3.3.1.tar.gz")
sha256sums=('3f2aed4cca299331c5f6ebc79b900e8d5374b6efe7422c343da7e28db9690d51')

build() {
  cd "$srcdir/nd2reader-3.3.1"
  python3 setup.py build
}

package() {
  cd "$srcdir/nd2reader-3.3.1"
  python3 setup.py install --root=$pkgdir --optimize=1 --skip-build

  # make sure we don't install any world-writable or root-readable-only files
  # we shouldn't need to fix ownership as we extract tarballs as a non-root user
  # https://github.com/pypa/setuptools/issues/1328
  # https://github.com/LonamiWebs/Telethon/issues/1605
  chmod u=rwX,go=rX -R "$pkgdir"
  # make sure we don't install annoying files
  local _site_packages=$(python -c "import site; print(site.getsitepackages()[0])")
  rm -rf "$pkgdir/$_site_packages/tests/"
}

