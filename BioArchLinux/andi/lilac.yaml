maintainers:
  - github: kbipinkumar
    email: kbipinkumar@pm.me
build_prefix: extra-x86_64
pre_build_script: |
  run_cmd(['updpkgsums'])
post_build_script: |
  git_pkgbuild_commit()
repo_depends:
  - libdivsufsort
update_on:
  - source: github
    github: EvolBioInf/andi
    use_max_tag: true
    prefix: 'v'
