maintainers: 
  - github: starsareintherose
    email: kuoi@bioarchlinux.org
build_prefix: extra-x86_64
pre_build_script: |
  update_pkgrel()
  vcs_update()
post_build_script: |
  git_add_files('PKGBUILD')
  git_pkgbuild_commit()
  git_commit()
update_on:
  - source: gitlab
    gitlab: Archive/gconf
    host: gitlab.gnome.org
#    use_commit: true
