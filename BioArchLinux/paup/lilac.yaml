maintainers:
  - github: starsareintherose
    email: kuoi@bioarchlinux.org
build_prefix: extra-x86_64
pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
update_on:
  - source: regex
    url: "http://phylosolutions.com/paup-test/"
    regex: paup(\w+)_centos64.gz
repo_depends:
  - python2
