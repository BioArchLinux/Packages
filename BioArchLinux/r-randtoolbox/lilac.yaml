build_prefix: extra-x86_64
maintainers:
  - github: sukanka
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()
repo_depends:
  - r-rngwell
update_on:
  - regex: randtoolbox_([\d._-]+).tar.gz
    source: regex
    url: https://cran.r-project.org/package=randtoolbox
  - alias: r
