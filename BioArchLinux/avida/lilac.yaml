maintainers:
  - github: sukanka
    email: su975853527@gmail.com
build_prefix: extra-x86_64
pre_build_script: |
  run_cmd(['updpkgsums'])
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()
update_on:
  - source: github
    github: devosoft/avida
    use_max_tag: true
    prefix: 'v'
